# Pinned copies from the PO.DAAC provider bundle (OKF SPEC 5.7). Checked
# and refreshed by tools/sync_check.py in the canonical repository:
#   uv run tools/sync_check.py ../hydrology/knowledge                  # check at the pin
#   uv run tools/sync_check.py ../hydrology/knowledge --refresh COMMIT  # rewrite the copies
# The pin is the steward's signing commit; refresh at every plugin release.
source:
  repository: open-science-pillars/nasa-daac-knowledge
  bundle: knowledge/podaac
  commit: 24b27927c387
  date: 2026-09-04
copy_dir: snapshot-podaac
scope:
  include:                                # the GRACE set the grace-groundwater work reads
    - datasets/grace-fo-mascons.md
    - gotchas/grace-coastal-leakage.md    # the dataset links both gotchas;
    - gotchas/grace-gia-correction.md     # omit one and the check reports DANGLING
